using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class StatHandler : MonoBehaviour
{
    public BarScaler healthScaler, timeScaler;
    public Text barText;
    // Start is called before the first frame update
    
}
